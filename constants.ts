import { Video } from './types';

// Lista vazia conforme solicitado. O catálogo será preenchido pelo usuário.
export const MOCK_VIDEOS: Video[] = [];

export const APP_NAME = "Paulinha Hot";
export const CURRENCY = "R$";